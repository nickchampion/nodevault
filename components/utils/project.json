{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@platform/components.utils",
  "sourceRoot": "components/utils",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "components/utils/**/*.ts"
        ]
      }
    }
  },
  "tags": []
}
